{
  "name": "sampaio-advocacia-landing-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "check:js": "find src -name '*.js' -print -exec node --check {} \\;",
    "test": "vitest run"
  },
  "dependencies": {
    "resend": "^6.17.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
